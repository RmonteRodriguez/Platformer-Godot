extends Area2D

@onready var playerNode = get_node("/root/Main Scene/Player")

#func _input(event):
	#f get_overlapping_bodies().size() > 0:
		#playerNode.get_parent().remove_child(playerNode)
		#self.add_child(playerNode)
		#print(self.get_children())
